<Page
    x:Class="UWPTeamWork.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPTeamWork"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Loaded="Page_Loaded">

    <Page.Resources>
        <SolidColorBrush x:Key="ClockDaskBg_1" Color="#039065"/>
    </Page.Resources>
    <Grid x:Name="RootGrid" Background="{StaticResource ClockDaskBg_1}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="LayoutStyle">
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="768"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ClockLayer.(Grid.RowSpan)" Value="2"/>
                        <Setter Target="ClockLayer.Margin" Value="0"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid x:Name="ClockLayer"  Grid.Row="0" Grid.RowSpan="1" Grid.Column="1" HorizontalAlignment="Stretch" Margin="20,40,20,0">
            <Viewbox  MaxHeight="500" MaxWidth="500">
                <local:SlideClock x:Name="SlideClock" Background="{Binding ElementName=RootGrid,Path=Background,Mode=OneWay}" />
            </Viewbox>
        </Grid>
        <Button Grid.Row="1" Grid.ColumnSpan="3" x:Name="Stop" Width="50" Height="50" HorizontalAlignment="Left" VerticalAlignment="Bottom" Click="Stop_Click"/>
        <Button Grid.Row="1" Grid.ColumnSpan="3" x:Name="Resume" Width="50" Height="50" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="Resume_Click"/>
        <Button Grid.Row="1" Grid.ColumnSpan="3" x:Name="Start" Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Bottom" Click="Start_Click"/>
    </Grid>
</Page>
