<Page
    x:Class="UWPTeamWork.TimerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPTeamWork"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Loaded="Page_Loaded" MinWidth="100">

    <Page.Resources>
        <SolidColorBrush x:Key="ClockDaskBg_1" Color="#0aa065"/>
        <SolidColorBrush x:Key="TitleBar_Fg" Color="#ffffff"/>
        <x:Double x:Key="TitleBarText">16</x:Double>
    </Page.Resources>

    <Grid x:Name="RootGrid" Background="{StaticResource ClockDaskBg_1}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="LayoutStyle">
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="768"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="Title" Text="{Binding ElementName=SlideClock,Path=MainSeconds,Converter={StaticResource MinAngConverter}}" TextAlignment="Center" FontWeight="Light"
                   Foreground="{StaticResource TitleBar_Fg}" 
                   FontSize="{StaticResource TitleBarText}" 
                   Margin="10,0,0,0" />
        <Viewbox x:Name="ClockLayer"   Grid.RowSpan="2" Grid.ColumnSpan="3" MaxHeight="600" MaxWidth="600">
            <local:SlideClock x:Name="SlideClock" Background="{Binding ElementName=RootGrid,Path=Background,Mode=OneWay}" />
        </Viewbox>
    </Grid>
</Page>
